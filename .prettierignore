# Build output
dist/
.angular/
# Assembled by the Pages deploy's own step; built files, not source. Without this
# `prettier --check .` descends into them and takes `npm run lint` red the moment
# anyone runs that step locally.
site/

# Dependencies
node_modules/

# Repository-local Codex/agent instructions are maintained as tool metadata,
# not application source.
.agents/
.codex/

# Test coverage
coverage/

# Lockfile (managed by npm)
package-lock.json

# Vendored card SVGs — do not reformat
public/cards/

# iOS subproject — governed by SwiftFormat/SwiftLint, not prettier.
# (Includes the machine-written ios/Fixtures/*.json from the parity exporter.)
ios/

# Intentionally-untracked scratch file — never reformat or commit
docs/repo-current-state.md

# Playwright MCP session artifacts
.playwright-mcp/

# Playwright E2E output
playwright-report/
test-results/
