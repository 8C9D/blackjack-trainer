# PROD-READINESS

A bounded production-readiness sweep of this repository.
No features added. Baseline for every "green" claim: [`reviews/BASELINE.md`](reviews/BASELINE.md).

- Branch: `prod-readiness/2026-08-10`
- Base commit: `fc7d0c32de8e89f41fd3457a1e5bd014b40e43d5`
- Date: 2026-08-10

---

## Stage 0 context (established by inspection and execution, not assumption)

### Stack and commands

Two shipped artifacts live in one repository.

**Web** - Angular 22.1 (standalone, signals, `ChangeDetectionStrategy.Eager`, zoneless: `angular.json` declares no `polyfills`, so zone.js is absent), TypeScript 6.0 with `strict: true` (`tsconfig.json:6`) plus `noImplicitOverride`, `noPropertyAccessFromIndexSignature`, `noImplicitReturns` and `noFallthroughCasesInSwitch` (`tsconfig.json:7-10`) and `strictTemplates` (`tsconfig.json:22`).
Package manager npm 11.12.1, pinned by `package.json:22` (`packageManager`) and `.nvmrc` (Node 22; this machine ran Node 24.15.0, which satisfies `engines` at `package.json:23-25`).
SCSS styling, Vitest via the `@angular/build:unit-test` builder (`angular.json`, `vitest.config.ts`), Playwright for E2E.

Commands, taken from `package.json:4-20` and `.github/workflows/ci.yml`, each executed to confirm it works (results in `reviews/BASELINE.md`):

| Purpose            | Command                                                            | Confirmed                                    |
| ------------------ | ------------------------------------------------------------------ | -------------------------------------------- |
| Build              | `npm run build`                                                    | yes, exit 0                                  |
| Unit tests         | `npm test`                                                         | yes, 1526 pass                               |
| Coverage gate      | `npm run test:coverage`                                            | yes, 4/4 thresholds met                      |
| Lint               | `npm run lint` (= `tsc --noEmit` + `prettier --check`)             | yes, exit 0                                  |
| E2E                | `E2E_SERVER=dist npm run e2e`                                      | yes, 111 pass                                |
| Parity export gate | `npm run export:fixtures` + `git diff --exit-code -- ios/Fixtures` | yes, no drift                                |
| Dev server         | `npm start` (`ng serve`)                                           | not run (E2E used the `dist` server instead) |

**iOS** - SwiftUI, Swift 5 language mode, `SWIFT_STRICT_CONCURRENCY: minimal`, deployment target iOS 17, iPhone-only, project generated by XcodeGen from `ios/project.yml` (the `.xcodeproj` is committed).
Commands from `.github/workflows/ios-ci.yml`, all executed: `swiftformat --lint .` (0/105 files need formatting), `swiftlint lint` (clean), `xcodebuild -scheme BlackjackTrainer -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build test` (`** TEST SUCCEEDED **`, 335 tests).

There is no linter for TypeScript beyond `tsc` + Prettier - no ESLint in the tree. That is a deliberate-looking gap, not a finding I can act on.

### How this ships

Two independent channels, neither of which is a server:

1. **Web: a static site on GitHub Pages.** `.github/workflows/pages.yml` builds with `--base-href /blackjack-trainer/`, assembles `site/` from `dist/blackjack-trainer/browser/` plus the two legal pages plus a copy of the shell as `404.html`, and deploys via `actions/deploy-pages@v4` to `https://8c9d.github.io/blackjack-trainer/`.
2. **iOS: an App Store binary.** A paid iPhone app, `MARKETING_VERSION 1.0`, bundle id `com.arthurzhang.blackjacktrainer.app`, signed with `DEVELOPMENT_TEAM C3W798H8U8` (`ios/project.yml:20`). Submission is manual and owner-driven (`LAUNCH-CHECKLIST.md`).

There is no container, no server binary, and no published library.

### Boundaries actually present

Verified by grep over `src`, `tools`, `e2e` and `ios/BlackjackTrainer`:

| Boundary                          | Present?                     | Evidence                                                                                                                                                                                                                                                                              |
| --------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Network out (app code)            | **No**                       | zero matches for `fetch(`, `XMLHttpRequest`, `HttpClient`, `WebSocket`, `sendBeacon` in `src`; zero for `URLSession`/`URLRequest`/`http(s)://` in `ios/BlackjackTrainer`                                                                                                              |
| Network in                        | **No**                       | no server in the shipped artifacts                                                                                                                                                                                                                                                    |
| Service worker (own-origin fetch) | **Yes**                      | `src/app/app.config.ts:33-36`, `ngsw-config.json`                                                                                                                                                                                                                                     |
| Persistence                       | **Yes**                      | web `localStorage` only (`src/app/core/services/storage.ts`); iOS `UserDefaults` + `NSUbiquitousKeyValueStore`                                                                                                                                                                        |
| Filesystem                        | **Yes, user-initiated only** | backup export/import, `src/app/core/services/backup.service.ts`, `settings-page.component.ts:393-411`                                                                                                                                                                                 |
| Auth / accounts                   | **No**                       | no login anywhere                                                                                                                                                                                                                                                                     |
| Payments                          | **No**                       | paid up-front on the App Store; no in-app purchase code                                                                                                                                                                                                                               |
| Background jobs                   | **No**                       | only `setTimeout` drill advance timers, all cleared on destroy                                                                                                                                                                                                                        |
| Third-party APIs / SDKs           | **No**                       | no SDK is called from source. 9 direct runtime deps (`package.json:26-36`), all `@angular/*` + `rxjs` + `tslib`; the full prod tree also carries `zod@4.4.3` and `@standard-schema/spec@1.1.0` via `@angular/forms`, which no file under `src` imports, so neither reaches the bundle |
| Database / queue                  | **No**                       | -                                                                                                                                                                                                                                                                                     |

**Passes skipped for want of a boundary:** pass 3 (Data / migrations / restore against a throwaway database) - there is no database, no migration system, and no transaction boundary; the nearest analogue is localStorage schema coercion, which is covered under pass 2.
Most of pass 4 (timeouts, retries, backoff, idempotency) is likewise inapplicable: there is no dependency to be down except the app's own static origin.
Most of pass 5 (structured logging, error reporting, health signal) is inapplicable: a static client-side bundle has no log sink and no health endpoint, and adding either would be a new feature.

### What can be executed to verify a change

Everything in the table above runs locally and offline. Nothing in this repository requires a network resource to verify, with two exceptions recorded under CANNOT ASSESS: real iCloud multi-device behaviour, and App Store code-signing/provisioning.
This is not a P0 - the gates run clean (`reviews/BASELINE.md`).

### What "production" means here, and who breaks

Derived from `README.md`, `.github/workflows/pages.yml`, `LAUNCH-CHECKLIST.md` and `ios/AppStore/*.html`:

- **The user who breaks is a trainee**, and the thing they lose is practice history - streaks, accuracy, weak spots - held only in `localStorage` (web) or `UserDefaults`/iCloud KVS (iOS). There is no server copy and no account. `src/app/core/services/storage.ts:44-52` says this in the source itself.
- **The second party who breaks is the owner**, a single developer shipping a paid App Store binary and a public GitHub Pages site under their own name. A false published privacy statement or a broken store URL lands on them personally.
- There is no wagering, no money, no PII, no telemetry, and no third party receiving anything. That materially lowers the ceiling on how bad any defect here can be, and the severities below reflect that.

---

## Findings

`id | area | severity | evidence (file:line) | fix | blast radius`

### P0

None. See NOT DEFECTS and CANNOT ASSESS for the two candidates that were considered and did not survive.

### P1

| id       | area                           | severity | evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | fix                                                                                                                                                                                                                                                                                                                                                                | blast radius                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------- | ------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **W1**   | Build/deploy                   | P1       | `public/manifest.webmanifest:5-6` sets `"start_url": "/"` and `"scope": "/"`. `.github/workflows/pages.yml:37` builds with `--base-href /blackjack-trainer/` and `pages.yml:41-42` publishes that bundle as the site root. The manifest is copied verbatim by the asset pipeline - confirmed byte-identical between `public/manifest.webmanifest` and `dist/blackjack-trainer/browser/manifest.webmanifest` after a build. So an installed PWA launches at `https://8c9d.github.io/`, a different project's site on the shared origin (the shared-origin fact is asserted independently at `src/app/core/models/backup.model.ts:10-13`).                                                                                 | Make both values relative (`"./"`), matching the `icons[].src` entries in the same file, which are already relative and therefore already correct. Works at both `/` (dev, `serve-dist`) and `/blackjack-trainer/` (Pages).                                                                                                                                        | Two values in one static asset. Affects only PWA install/launch; no effect on the in-browser app. Untested today: `e2e/smoke/navigation.e2e.ts:114-130` asserts `display` and maskable icon sizes but never `start_url` or `scope`. **Review 0 condition:** the E2E test must assert the raw `start_url`/`scope` strings, not a resolved URL - under `serve-dist` the base href is `/`, so a resolved assertion would pass either way and the fix would ship unexercised. |
| **W2**   | Correctness / failure behavior | P1       | `src/app/core/services/app-update.service.ts:23-32` subscribes only to `swUpdate.versionUpdates`. `SwUpdate.unrecoverable` (declared at `node_modules/@angular/service-worker/types/service-worker.d.ts:447`) is never subscribed. When the worker reaches an unrecoverable state the app is broken across reloads and the user is told nothing; `src/app/app.ts:28-56` renders a banner only for `updateReady`.                                                                                                                                                                                                                                                                                                         | Subscribe to `unrecoverable` and surface it through the shell's existing banner as a **user-initiated** reload. Deliberately not an automatic reload: an unrecoverable worker still controls the page, so a reflexive reload risks a loop.                                                                                                                         | One service + one shell branch. **Review 0 ruled this IN SCOPE**, with conditions: reuse the existing `aside.update` banner and its existing reload/dismiss controls, add no new component, injectable, config key or persisted key, keep the reload user-initiated, and add a unit test that pushes an `UnrecoverableStateEvent` through the service. If the fix grows a new component or user setting it has become a feature and must be reverted to DEFERRED.         |
| **B1**   | Correctness / resource         | P1       | `tools/serve-dist.mjs:32` calls `decodeURIComponent(...)` outside any `try`, inside an `async` request handler. A malformed percent-escape throws `URIError`, which becomes an unhandled rejection and **exits the process**. Reproduced: `curl http://127.0.0.1:4321/%.js` → server dies, `Node.js v24.15.0 ... URIError: URI malformed at serve-dist.mjs:32:26`, every subsequent request returns `000`.                                                                                                                                                                                                                                                                                                               | Wrap **only** the URL parse/decode so a malformed request returns 404 instead of killing the server. **Review 0 condition:** do not wrap the whole handler in one `try` - that would satisfy the finding while hiding every other error the handler can raise; the existing 404 catch at lines 45-48 stays separate, and the reproduction command is the artifact. | One file, dev/CI only - this server never runs in production (GitHub Pages does). It is the Playwright `webServer` for `E2E_SERVER=dist`, which is what CI uses (`.github/workflows/ci.yml`), so a crash presents as a mass of unrelated E2E failures. Rated P1 not P2 for that reason - an undiagnosable release gate - and not P0 because no user-facing path reaches it.                                                                                               |
| **D1**   | Docs / compliance              | P1       | `ios/AppStore/privacy.html:65` and `ios/AppStore/support.html:55` both publish `mailto:CONTACT_EMAIL_HERE` as the contact address. `.github/workflows/pages.yml:42` copies both files into the deployed site, and they are the declared App Store privacy and support URLs (`pages.yml:6-8`). `LAUNCH-CHECKLIST.md:59` and `:165` confirm this is the one placeholder left for the owner. **Review 0 tense correction:** the pages are not published yet - Pages source has not been flipped to GitHub Actions (owner action O4, `LAUNCH-CHECKLIST.md:59`), so the accurate statement is that they _would_ publish on the first deploy. Severity unchanged: App Store submission needs a working support URL either way. | Replace with the real support address.                                                                                                                                                                                                                                                                                                                             | Two lines in two published legal pages. **Cannot be fixed in this run**: the address is a fact only the owner holds, this run may not ask questions, and inventing a plausible address on a live privacy policy is strictly worse than leaving the placeholder visible. See DEFERRED.                                                                                                                                                                                     |
| **I1**   | Data / iOS sync                | P1       | `ios/BlackjackTrainer/Stores/CloudKeyValueStore.swift:63-72`: `synchronize()` does not wait for an iCloud download, so on a device whose KVS cache has not yet populated, `cloud.data(forKey:) == nil` and the branch at line 70 calls `pushToCloud()`, writing this device's empty state over the shared key. Adoption is last-writer-wins (`SessionStatsStore.adoptFromCloud`, `Stores/StatsStore.swift:74-80`; the line that replaces local state wholesale is `stats = value` at `:78`). Wiring is live for all nine stores: `ios/BlackjackTrainer/App/AppModel.swift:49-78`.                                                                                                                                        | A correct fix is merge semantics on adoption, or deferring the seed until the initial-sync notification has been observed.                                                                                                                                                                                                                                         | **Cannot be fixed in this run**: last-writer-wins is a recorded design decision (D5) and replacing it changes user-visible sync behaviour, i.e. a feature. Narrowing the launch seed alone would relocate the race to the first recorded rep rather than remove it. See DEFERRED. Rated P1 rather than P0 despite being data loss, because the capability is unprovisioned today (`LAUNCH-CHECKLIST.md:22-23`), which makes the path inert in the shipped build.          |
| **R0-4** | Tests / release gate           | P1       | Added by Review 0. `playwright.config.ts:37` sets `reuseExistingServer: !process.env.CI`, so when anything already listens on `127.0.0.1:4200`, `E2E_SERVER=dist npm run e2e` silently attaches to it instead of starting `tools/serve-dist.mjs` - and the suite then tests whatever that server serves. The reviewer reproduced this with `ng serve` bound to 4200: `E2E_SERVER=dist npm run e2e -- e2e/smoke/navigation.e2e.ts` reported `13 passed` while `serve-dist` never started. Playwright does not pipe `webServer` stdout, so no run log distinguishes the two servers after the fact.                                                                                                                        | Make the `dist` server path fail rather than silently borrow a foreign server.                                                                                                                                                                                                                                                                                     | One test-config file. Worse than B1 in kind: B1 fails loudly, this one **fails green**. CI is immune (`CI=true` disables reuse, `.github/workflows/ci.yml:48-50`), but the whole of `reviews/BASELINE.md` was captured through this gate on a developer machine. `playwright.config.ts` is test configuration, not CI/CD or deploy configuration, so a fix is permitted. Not P0: no user-facing path, no data.                                                            |

### Regression introduced and fixed inside this run (not a work-list item)

| id       | what                                                                                                                                                                                                                                                                                                                                                                                                                                                             | resolution                                                                                                                                                                                                                                                                               |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **R0-1** | Review 0 found that the two stage-0 commits themselves broke `npm run lint`: `prettier --check .` flagged `PROD-READINESS.md` and `reviews/BASELINE.md` (markdown table padding), exit 1 against BASELINE's 0, with `.github/workflows/ci.yml:19` running that gate on every push to `main`. Cause: the gates were captured for the baseline and never re-run after the artifacts were written, so the stage's one changed path was the one path never verified. | Fixed in commit `45f41bc` by running `npx prettier --write` on both files. `npm run lint` re-verified at exit 0. Per the run's rules a defect introduced by this run's own changes is a regression, not a finding, and is fixed in the stage that caused it rather than carried forward. |

### P2 (documented, not fixed)

| id   | area              | evidence                                                                                                                                                                                                                        | note                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P2-1 | Dependencies      | `npm audit`: 3 moderate, all one chain - `@angular/cli` → `@modelcontextprotocol/sdk` → `@hono/node-server` <2.0.5 (GHSA-frvp-7c67-39w9, path traversal in `serve-static` on **Windows** via encoded backslash).                | Dev-only. Verified against `npm ls --omit=dev --all`: the production tree is 9 direct deps plus `zod` and `@standard-schema/spec` under `@angular/forms`; none is in the advisory chain. The only offered fix is `@angular/cli@21.0.4`, a **major downgrade** - prohibited by this run's scope, and disproportionate to a Windows-only dev-server advisory.                                             |
| P2-2 | Build             | `npm run build` warns: `src/app/features/chart/chart-page.component.scss exceeded maximum budget. Budget 5.00 kB was not met by 368 bytes with a total of 5.37 kB` (budget at `angular.json`, `anyComponentStyle`).             | A warning, not an error. Present in BASELINE, so it is inherited, not introduced. Fixing it means either editing the budget (weakening a gate) or refactoring a stylesheet outside any finding.                                                                                                                                                                                                         |
| P2-3 | Failure behavior  | `src/main.ts:8` - `bootstrapApplication(...).catch((err) => console.error(err))`. `src/index.html:34` contains a bare `<app-root></app-root>` with no fallback content and no `<noscript>`.                                     | A bootstrap failure (e.g. a lazy chunk 404 after a redeploy) yields a permanently blank page whose only signal is the devtools console. Out of scope, and Review 0 (R0-6) restated the reason: not "user-visible copy is a feature", but that the fix adds a static element to `index.html` that no existing surface owns. A later run wanting to fix it needs a fresh scope ruling, not this sentence. |
| P2-4 | Security headers  | No `Content-Security-Policy` anywhere: `src/index.html` has no `<meta http-equiv>`, and GitHub Pages cannot set response headers.                                                                                               | Low value here: the prior audit (`docs/security-pass-2026-08-06.md`) records zero unsafe sinks - no `innerHTML`, `eval`, `new Function`, or `bypassSecurityTrust*` in `src` - and the app loads no third-party code. Adding a CSP meta is a hardening change, not a fix for a defect.                                                                                                                   |
| P2-5 | Offline           | `ngsw-config.json` caches `/index.html`, `/*.css`, `/*.js`, `/cards/*.svg` and image/font extensions. The two published legal pages (`privacy.html`, `support.html`, added to the site by `pages.yml:42`) match no asset group. | Those pages 404 offline. They are App Store URLs read online; the app itself never links to them.                                                                                                                                                                                                                                                                                                       |
| P2-6 | Typing            | `src/app/core/services/flow-prefs.service.ts:213` - `COUNTING_SYSTEMS.find(...)!` non-null assertion.                                                                                                                           | Safe today: `systemId` on line 208 is constrained by `oneOf` to a member of `COUNTING_SYSTEMS` or the default `'hi-lo'`, which is a member. It would become a crash only if `'hi-lo'` were removed from the data.                                                                                                                                                                                       |
| P2-7 | Resource          | `src/app/features/settings/settings-page.component.ts:402` - `await file.text()` with no size check on a user-chosen file.                                                                                                      | A multi-GB pick would exhaust the tab. Self-inflicted, one user, one tab, recoverable by closing it.                                                                                                                                                                                                                                                                                                    |
| R0-2 | Ledger accuracy   | Added by Review 0: nine evidence citations in the first draft of this ledger pointed at the wrong lines, one at a line that does not exist (`src/app/app.config.ts:38-41` in a 38-line file).                                   | All nine corrected in this commit, each re-derived from the file at this commit rather than taken from the reviewer's word. No finding's substance or severity changed. Cause: line numbers written from memory of a read and never re-checked after the surrounding prose was edited.                                                                                                                  |
| R0-6 | Scope consistency | Added by Review 0: W2 and P2-3 were given contradictory scope rulings on the same kind of change (`PROD-READINESS.md`, W2 row vs P2-3 row).                                                                                     | Resolved by the reviewer's ruling, now reflected in both rows: W2 is in scope because the banner that would carry it already exists; P2-3 stays out of scope because its fix adds a static element no existing surface owns.                                                                                                                                                                            |

---

## ASSUMPTIONS

1. **The working tree counted as clean.** `git status --porcelain` showed exactly two untracked directories, `.agents/` and `.codex/`, containing only agent skill definitions and a Codex config - no application code. `git diff` and `git diff --cached` were both empty. I read "uncommitted changes" as modified or staged **tracked** files and continued. Mitigation: no commit in this run uses `git add -A`; every commit names its paths, so those directories cannot be swept in.
2. **The tool sandbox was disabled for every build/test command.** The first sandboxed `npm run build` died with exit 134 (SIGABRT) and no diagnostic. Recorded in `reviews/BASELINE.md`.
3. **The Pages path is `/blackjack-trainer/`**, taken from `.github/workflows/pages.yml:37` and the URL in `ios/AppStore/support.html:45`. I could not confirm the site is actually live (that would require a network fetch to a non-local host).
4. **"Production" covers both artifacts.** The prompt says "this repository"; the repository ships a web app and an iOS app, so both were swept.
5. **The iOS iCloud capability is unprovisioned**, per `LAUNCH-CHECKLIST.md:22-23` and `TRIM-REPORT.md:70`. This is an out-of-repo portal setting I cannot verify. It is load-bearing for finding I1's severity and for the NOT-DEFECTS ruling on the privacy text; if it is wrong, both change.
6. **`public/manifest.webmanifest` is an application asset, not deploy configuration**, so editing it does not violate the prohibition on changing deploy config. It ships inside the bundle and is served to the browser; the deploy configuration is `pages.yml`, which I have not touched.

## DEFERRED

| id     | reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D1** | Needs a fact only the owner holds (the support email address). This run may not ask questions, and the conservative option on a published privacy policy is to leave a clearly-marked placeholder rather than invent an address that would silently swallow user mail. Already tracked as owner action O4 in `LAUNCH-CHECKLIST.md:59`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **I1** | A correct fix replaces last-writer-wins with merge semantics on adoption, which changes user-visible sync behaviour and therefore is a feature under this run's scope rule. The available narrow fix - skip the launch seed when local state is empty - would move the race to the first recorded rep instead of removing it, which is the "relocated rather than removed" failure mode this run is explicitly meant to avoid. Recorded in full so the next run starts from it. **Review 0 addition, load-bearing for the next run:** `LAUNCH-CHECKLIST.md:22` records that provisioning turns sync on **without an app update**. So the already-shipped binary becomes a P0 data-loss path the day the owner flips a portal switch, with no code change to trigger a re-review. Whoever provisions iCloud must fix this first. |

## NOT DEFECTS

- **`tools/serve-dist.mjs:33-34`'s path-traversal claim is true.** The comment asserts `normalize()` prevents escaping `ROOT`. Verified by execution: `/%2e%2e/%2e%2e/%2e%2e/package.json` → 404, and `--path-as-is /../../package.json` → 404. `new URL(...).pathname` always begins with `/`, and `normalize` collapses leading `..` on an absolute path. No finding.
- **The App Store privacy text is accurate as shipped.** `ios/AppStore/privacy.html:48-50` and `support.html:45` say "This version of the app does not sync", while `AppModel.swift:49` does construct a real `UbiquitousKeyValueStore`. With the capability unprovisioned (assumption 5), `NSUbiquitousKeyValueStore` behaves as a local store and no data leaves the device, so the statement holds. Recorded because the statement's truth depends on an out-of-repo portal setting rather than on anything in this tree - a coupling worth knowing about, but not a code defect.
- **`ios/project.yml:20` `DEVELOPMENT_TEAM: C3W798H8U8` is not a secret.** An Apple Team ID is public in every distributed binary's provisioning profile and grants nothing on its own.
- **`ios/BlackjackTrainer/App/AppModel.swift:95` `preconditionFailure` on bundled-data load failure** is a deliberate fail-loud on a build-integrity invariant that CI gates (`ios/BlackjackTrainerTests`, plus the parity fixture check). Crashing on a corrupt build beats running with silently wrong strategy charts in a trainer whose entire value is being right.
- **`BackupService.restore` on a valid but empty backup wipes local data** (`src/app/core/services/backup.service.ts:83-126`). That is the documented semantic - a restore is the state the backup captured, not a merge - and `build()` legitimately produces `data: {}` on a fresh browser. Correct as written.
- **The double filter on backup keys is defence in depth, not a bug.** `parseBackup` admits any `blackjack-`-prefixed key (`backup.model.ts:96-103`) while `replaceNamespace` writes only declared `BACKUP_KEYS` (`backup.service.ts:130-135`). The wider parse and the narrower write are both intentional.
- **Drill timers do not leak.** All three `setTimeout` owners register `DestroyRef.onDestroy(() => this.clearAdvance())`: `basic-strategy-drill-page.component.ts:275`, `deviations-drill-page.component.ts:316`, `card-counting-page.component.ts:439`.

## CANNOT ASSESS

- **Whether the declared-but-unprovisioned iCloud entitlement (`ios/BlackjackTrainer/BlackjackTrainer.entitlements`) blocks archive/export signing.** Determining this requires Apple's developer portal and notarisation services - non-local resources this run may not contact. Worth flagging: `.github/workflows/ios-ci.yml` builds only for the **simulator**, which does not sign, so no gate in this repository exercises the signed device build at all. Fixing that would be a CI change, which this run may only report.
- **Real iCloud multi-device behaviour** (finding I1). Requires two provisioned devices and Apple's servers.
- **Whether the GitHub Pages site is live and serving** the layout `pages.yml` describes. Requires a network fetch to a non-local host.
- **Cross-browser behaviour of `URL.revokeObjectURL` immediately after `anchor.click()`** in `src/app/core/services/backup.service.ts:70-74`. It works in Chromium (`e2e/smoke/backup.e2e.ts` passes against the real build), and Chromium is the only browser configured in `playwright.config.ts`. Whether Firefox or Safari cancel the download when the object URL is revoked in the same task is not something this repository's tooling can determine, so no finding is asserted either way.

## NEXT ROUND

Findings discovered after the work list froze at Review 0. **Not fixed in this run, regardless of severity.** Recorded with full evidence so the next run starts from them.

| id     | severity | evidence                                                                                                                                                                                                                                                                                                                                                                                | why it is not in this run                                                                                                                                                                                                                                                                                                                 |
| ------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **N1** | P2       | Added by Review 0 (R0-5). `.github/workflows/pages.yml:12-15` and `.github/workflows/ci.yml:3-7` both fire independently on push to `main`. `pages.yml` runs only `npm ci` and `npm run build` before deploying: never the lint gate, the unit tests, the coverage gate, the parity anti-drift gate, or E2E. Any failure that is not a build failure publishes to the live site anyway. | Fixing it means editing a CI/CD workflow file, which this run may only report. For a trainer whose value is being right about strategy charts, the parity and unit gates are exactly the ones that do not guard the deploy.                                                                                                               |
| **N2** | P2       | Added by Review 0 (R0-3), verified independently here with `npm ls --omit=dev --all`: `@angular/forms@22.1.0` is a declared runtime dependency that no file under `src` imports (`grep -rn "@angular/forms" src` returns nothing). It pulls `zod@4.4.3` and `@standard-schema/spec@1.1.0` into the production tree.                                                                     | Removing a dependency is a dependency change, which this run's scope forbids except to patch a CVE on the work list. Neither package reaches the shipped bundle and neither is in the advisory chain, so nothing is at risk today.                                                                                                        |
| **N3** | P2       | Added by REVIEW-pass2. Nothing tests `tools/` at all: `tsconfig.spec.json:9` scopes specs to `src`, so neither `tools/serve-dist.mjs` nor `tools/export-parity-fixtures.ts` has a test - and the latter feeds the CI parity anti-drift gate that keeps the iOS app in lockstep with the web engines.                                                                                    | Pre-existing, discovered after the work list froze, not a regression from this run's own changes. Adding a test target for `tools/` is scaffolding beyond any frozen finding.                                                                                                                                                             |
| **N4** | P2       | Added by REVIEW-pass4 (F4-2), measured on `/drill/basic-strategy` at 375x700: with the update banner shown, `document.documentElement.scrollHeight === clientHeight === 700` (the page does not scroll) and `elementFromPoint` at the centre of the Hit button returns a `SPAN` inside `.update`. All six action controls intersect the banner box.                                     | Pre-existing: the update-ready banner has always covered those controls and this run did not change that. Fixing the overlap means reworking the banner's layout or the drill's, which no frozen finding cites. The recovery state's specific version - no "Later" to press - is recorded as an accepted trade in `reviews/ARTIFACTS.md`. |
| **N5** | P2       | Added by REVIEW-pass6 (F6-4). No gate anywhere builds or serves the `--base-href /blackjack-trainer/` bundle that `.github/workflows/pages.yml:37` actually deploys. The E2E suite runs the root-href build, so the manifest protection added for W1 is a raw-string comparison rather than a check on what the deployed configuration resolves to.                                     | Pre-existing, and closing it means a new test lane or a CI change - both beyond any frozen finding. Note this is what let W1 exist unnoticed in the first place.                                                                                                                                                                          |
| **N6** | P2       | Added by REVIEW-pass6 (F6-1) and reproduced independently: `public/manifest.webmanifest` declares no `id`, so the PWA's application identity falls back to `start_url`. Confirmed via CDP `Page.getAppManifest` - computed `id` moved from `http://host/` to `http://host/blackjack-trainer/` when W1 was fixed.                                                                        | Adding an `id` is a new config key, which this run's scope forbids, and it would pin identity to the broken value. Harmless today because Pages is not live (owner action O4, `LAUNCH-CHECKLIST.md:59`), so no installed copy carries the old identity. It stops being harmless if the app is published before the W1 fix ships.          |

---

## Termination: the frozen work list

The P0 and P1 findings surviving Review 0 are the work list, and it is frozen here.
Six findings, under the 15 cap, so nothing was trimmed to NEXT ROUND for capacity.
All four are P1, so the blast-radius tie-break (smallest first) that decides what survives
a trim decides nothing here - nothing was trimmed. Execution therefore follows the numbered
pass order, which is the structure the run is defined by.
An earlier draft of this table listed the blast-radius order as the execution order; that
was a misreading of the rule, which governs trimming, and is corrected here.
**Provenance, per REVIEW-pass4 (F4-6):** that correction was made in `ac6a523`, the same
commit that executes W2 - i.e. the commit that benefits from moving W2 earlier in the table.
The reviewer checked the substance independently and accepted it (the previous sentence was
self-contradictory: it claimed the blast-radius order "is also the pass order below" while
the pass column read 2, 7, 6, 4). No finding was added, removed, re-severitied or
re-targeted, and W2 is assigned to pass 4 under both versions. This note is the independent
record that a document declared frozen was rewritten.

| order | id   | pass                      | terminal state target |
| ----- | ---- | ------------------------- | --------------------- |
| 1     | B1   | Pass 2 - correctness      | RESOLVED              |
| 2     | W2   | Pass 4 - failure behavior | RESOLVED              |
| 3     | W1   | Pass 6 - build and deploy | RESOLVED              |
| 4     | R0-4 | Pass 7 - tests            | RESOLVED              |
| -     | D1   | -                         | DEFERRED              |
| -     | I1   | -                         | DEFERRED              |

**Passes skipped because no frozen finding touches them:** pass 1 (secrets / authn / injection / vulnerable deps - the tree is clean, and the only advisory is a dev-only Windows path traversal whose fix is a prohibited major downgrade), pass 3 (data - no persistence boundary of the kind this pass describes, and the one data finding, I1, is deferred), pass 5 (observability - a static client-side bundle has no log sink and no health endpoint, and both would be features).

---

## Status

Terminal states are RESOLVED (with artifact evidence), DEFERRED (with reason), or REJECTED TWICE (reverted, objection recorded).

| id   | status                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| B1   | RESOLVED - artifact in [`reviews/ARTIFACTS.md`](reviews/ARTIFACTS.md#b1---toolsserve-distmjs-exits-the-process-on-a-malformed-request-url) (before/after reproduction), verdict in `reviews/REVIEW-pass2.md`                                                                                                                                                                                                             |
| R0-4 | pending                                                                                                                                                                                                                                                                                                                                                                                                                  |
| W1   | RESOLVED - artifact in [`reviews/ARTIFACTS.md`](reviews/ARTIFACTS.md): Chrome's own `Page.getAppManifest` resolving the installed start URL to the site root before and to the app after, under a real Pages-base-href build; plus proof the new E2E assertion fails on the old manifest.                                                                                                                                |
| W2   | RESOLVED - artifact in [`reviews/ARTIFACTS.md`](reviews/ARTIFACTS.md). Verified end to end against a genuinely damaged service worker, induced locally, plus proof the behavioural tests fail when only the subscription is removed. An earlier version of this row claimed the state could not be induced here; REVIEW-pass4 (F4-1) struck that claim and it was reproduced independently before the row was corrected. |
| D1   | DEFERRED (see above)                                                                                                                                                                                                                                                                                                                                                                                                     |
| I1   | DEFERRED (see above)                                                                                                                                                                                                                                                                                                                                                                                                     |
