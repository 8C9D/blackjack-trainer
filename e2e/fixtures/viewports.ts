// Named viewports for the responsive specs. PHONE is below the app's 600px
// breakpoint (styles.scss) where key-cap hints collapse; DESKTOP is above it.
export const DESKTOP = { width: 1024, height: 768 };
export const PHONE = { width: 390, height: 844 };
